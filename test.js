// function addofsquare (x, y) {
//     addition = (x*x) + (y*y) 
//     return addition
// }
// console.log(addofsquare(2,3));

function maxno (x,y,z){
  
    if (x>y && x>z) {
        console.log("x is greatest no:",x);
       
    } else if (y>x && y>z) {
        console.log("y is greatest no:",y);
       
    } else {
        console.log("z is greatest no:",z);
      
    }
    }
    maxno(5,3,2);
